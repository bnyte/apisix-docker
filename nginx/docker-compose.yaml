version: "3"

services:
  nginx:
    image: latest
    restart: always
    volumes:
      - ./upstream/:/etc/nginx/conf.d/
      - /opt/ssl/nginx/:/opt/ssl/nginx/
    ports:
      - 80:80
      - 443:443